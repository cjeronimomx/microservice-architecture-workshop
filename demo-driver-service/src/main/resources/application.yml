#TODO 0. Create the folder demo-config-properties into root git repository
#TODO 1.1 Create demo-driver-service folder into demo-config-properties folder
#TODO 1.2 Copy this application-work.yml file into demo-driver-service folder
#TODO 1.3 Push git changes
spring:
  application:
    name: demo-driver-service
  banner:
    charset: UTF-8
    location: classpath:banner.txt

server:
  port: 4040
  servlet:
    context-path: /demo
    application-display-name: ${spring.application.name}
