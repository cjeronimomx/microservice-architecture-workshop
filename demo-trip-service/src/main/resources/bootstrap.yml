#TODO: 4.2 Add properties into demo-trip-service/application-work.yml in demo-config-properties to customize actuator
spring:
  application:
    name: demo-trip-service
  banner:
    charset: UTF-8
    location: classpath:banner.txt

  cloud:
    config:
      fail-fast: true
      uri: http://127.0.0.1:4050/demo/config
      username: config
      password: Secret

server:
  port: 4042
  servlet:
    context-path: /demo
    application-display-name: ${spring.application.name}

